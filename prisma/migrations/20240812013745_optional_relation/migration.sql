BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ShortUrl] ALTER COLUMN [user_id] NVARCHAR(255) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
